# 项目结构导航

> 更新日期：2026-07-24  
> 当前 IMU 方案：ICM20602 + ESP32-S3 SuperMini，**5 节点**（腰部、左/右膝、左/右脚踝）。  
> 数据链路：NodePreset 配置驱动（`full_body_9` / `lower_body_5`），不硬编码节点数。
> 
> 你（主程）只维护 Python 端（`skating-deep-cnn-lstm-bayes/`），  
> 前端/云函数由队友维护，除非必要不修改。
 
---

## 一、根目录关键文件

| 文件 | 用途 |
|---|---|
| `project.config.json` | 微信小程序项目配置 |
| `project.private.config.json` | 私有配置，开发时 `urlCheck: false` |
| `config.js` | 小程序前端配置 — 云环境、WiFi/BLE 配置、5 节点传感器 profile |
| `.instructions.md` | **GitHub Copilot 工作守则** — 每次修改前先 git commit |
| `项目结构导航.md` | 本文件 |

### 配置文件

| 文件 | 用途 |
|---|---|
| `calibration_profiles_v3_raw.json` | V3 腰部试验台基准（原始偏置） |
| `calibration_profiles_v3_raw_per_node.json` | V3 四子节点试验台基准 |

### 不需要的根目录文件

| 文件 | 说明 |
|---|---|
| `ext.json` | 遗留，`extEnable: false` |
| `微信二维码.png` | 小程序二维码，不影响运行 |
| `dong_change/` | 队友改动备份（`src/` + `tool/`），仅参考，不参与运行 |
| `database/` | MySQL schema 备份 |
| `database_export-*.json` | 数据库导出备份，非必要 |

---

## 二、Python AI 推理端 — `skating-deep-cnn-lstm-bayes/`

**这是你唯一维护的目录。** 统一 FastAPI 服务（传感器桥接 MQTT + 深度学习推理）。

### 启动

```powershell
cd skating-deep-cnn-lstm-bayes
D:\py_project\new_competition\.venv\Scripts\python.exe -m uvicorn src.sensor_api:app --host 0.0.0.0 --port 18080
```

### 目录结构

```
skating-deep-cnn-lstm-bayes/
├── requirements.txt                # Python 依赖（torch, fastapi, paho-mqtt, lightgbm, scipy）
│
├── src/
│   ├── sensor_api.py               # ★ 唯一 FastAPI 主入口
│   │                                #   路由：/health /frames /calibration/gyro
│   │                                #         /denoise/training-frames /infer
│   ├── predict.py                  # 推理核心 — load_action_model / predict_record
│   ├── model.py                    # 模型定义 — 分类器 + 结构感知编码器 + SSL
│   ├── jsonl_sequence_dataset.py   # JSONL 数据加载 + NodePreset + SequenceConfig
│   │
│   ├── denoise.py                  # IMU 去噪 — Hampel 野值剔除 + 低通 + 校准
│   ├── training_denoise.py         # 去噪适配层
│   ├── attitude.py                 # 姿态解算（Mahony 互补滤波）
│   ├── mqtt_client.py              # MQTT 订阅、帧缓存、校准命令
│   │
│   ├── train_multiclass.py         # ★ 动作分类训练（baseline / structured）
│   ├── train_lgb_quality.py        # ★ LightGBM 质量评分训练（primary）
│   ├── train_ssl.py                # 自监督预训练
│   ├── train_action.py             # 旧版分类训练（推荐用 train_multiclass）
│   ├── train_bayes_quality.py      # GaussianNB 质量训练（legacy）
│   │
│   ├── coach_feedback.py           # 教练反馈引擎
│   ├── quality_labels.py           # 评分标签转换
│   ├── similarity_scoring.py       # 参考库相似度评分
│   ├── build_reference_library.py  # 构建参考动作库
│   │
│   ├── api.py                      # ⚠️ 已废弃 — 功能已融合到 sensor_api.py
│   └── __init__.py
│
├── experiments/                    # 训练产物
│   ├── weight_shift_v2/            # action_model.pt（CNN-LSTM, 旧版）
│   ├── lgb_quality_v3/             # lgb_quality_model.pkl（LightGBM v3）
│   ├── multiclass_structured/      # 结构感知多分类（best val macro_f1=1.0）
│   ├── multiclass_baseline/        # 基线多分类对比
│   ├── lgb_quality_multiclass/     # 多分类质量模型
│   └── weight_shift_ssl_v1/       # SSL 预训练版
│
├── tools/                          # 离线脚本
│   ├── build_calibration_profiles.py   # 构建校准 profile
│   ├── build_multiclass_dataset.py     # 构建多分类数据集
│   ├── clean_database_export.py        # 数据库导出清洗
│   ├── augment_data.py                 # 数据增强
│   ├── analyze_static_calibration.py   # 静止校准分析
│   ├── analyze_static_imu.py           # 静止 IMU 质量分析
│   ├── retrain_models.py              # 模型重训练
│   ├── split_train_val_test.py        # 数据集切分
│   └── train_quality_multiclass.py    # 多分类质量训练
│
├── tests/
│   ├── test_denoise.py
│   ├── test_training_denoise.py
│   └── test_coach_feedback.py
│
└── vision/                         # 视觉融合（预留，含 pose/st-gcn 骨架）

```

### 路由总表（`sensor_api.py`）

| 路由 | 方法 | 说明 |
|---|---|---|
| `/health` | GET | MQTT 状态 + 模型加载状态 + sensor_mode |
| `/frames` | POST | 实时传感器帧轮询（支持多节点同步组装） |
| `/calibration/gyro` | POST | 下发陀螺仪校准命令（仅对在线节点） |
| `/calibration/gyro/clear` | POST | 清除固件 RAM 临时 offset |
| `/denoise/training-frames` | POST | 尖峰检测 + 低通滤波 |
| `/infer` | POST | 云函数兼容推理（CNN-LSTM + LightGBM） |

### 模型路径（环境变量可覆盖）

| 模型 | 默认路径 | 用途 |
|---|---|---|
| 动作分类 | `experiments/weight_shift_v2/action_model.pt` | 5/9 节点 IMU → 动作分类 |
| LightGBM 质量 | `experiments/lgb_quality_v3/lgb_quality_model.pkl` | 特征 → 连续 0-100 分数 |

### 训练命令（5 节点新数据用）

```powershell
# 动作分类（结构化编码器）
python -m src.train_multiclass ^
  --data-dir data_5node ^
  --arch structured ^
  --node-preset lower_body_5 ^
  --sequence-length 350 ^
  --output-dir experiments/multiclass_5node_v1 ^
  --max-epochs 100 --patience 15

# LightGBM 质量评分
python -m src.train_lgb_quality ^
  --raw data_5node/train.jsonl ^
  --quality-dataset data_5node/quality_scores.csv ^
  --action-model experiments/multiclass_5node_v1/action_model.pt ^
  --output-dir experiments/lgb_quality_5node_v1
```

---

## 三、微信小程序 — `miniprogram_hers/`

**你不维护此目录（除非必要）。** 队友维护。

### 配置

```text
miniprogram_hers/
├── config.js                    # WiFi/BLE/传感器 profile 配置
├── envList.js                   # 云环境列表
├── app.js                       # 登录持久化、设备 SDK、云初始化
├── app.json                     # 页面注册 + 插件 + TabBar
├── app.wxss                     # 全局样式
│
├── pages/
│   ├── login/                   # 登录页
│   └── coach/                   # ★ 教练端
│       ├── index/               # 教练首页（功能入口 + 智能教练）
│       ├── sensor/
│       │   ├── debug/           # ★ 训练数据采集页（WiFi/BLE 连接、采集、分析、保存）
│       │   ├── static-capture/  # 腰部静止温漂采集
│       │   ├── static-capture-four-nodes/ # 四节点静止采集
│       │   ├── display/         # 传感器数据展示
│       │   └── samples/list/    # 训练样本列表
│       ├── smart-coach/         # ★ 实时分析（独立页面，一键采集+推理+展示）
│       ├── inference-test/      # BLE 实时推理测试（旧）
│       ├── inference/history/   # 推理历史记录
│       ├── predict-result/      # 预测结果详情
│       └── ...                  # 其他队友模块（班级/认证/活动等）
│
├── utils/
│   ├── sensor-model.js           # SENSOR_ROLES（含 waist）、帧标准化、analyzeSensorSession()
│   ├── device-sensor-adapter.js  # /frames 采集、5 节点组帧
│   ├── remote-predict.js         # 远程模型调用 → predictAnalysis 云函数
│   ├── wearable-device-sdk.js    # BLE 穿戴设备 SDK
│   ├── inference-api.js          # 推理 API 封装
│   ├── feature-gates.js          # 功能开关
│   └── ...
│
├── components/                   # 公共组件
├── custom-tab-bar/               # 自定义 TabBar
└── images/                       # 图标
```

### 两个操作入口

| 入口 | 路径 | 用途 | 操作步骤 |
|---|---|---|---|
| **传感器调试** | `sensor/debug` | 采集训练数据 | 连 WiFi → 采集数据 → 填评分 → 保存 |
| **智能教练** | `smart-coach` | 比赛演示/实时分析 | 连 WiFi → 开始分析 → 自动出结果 |

---

## 四、云函数 — `cloudfunctions/`

**你不维护此目录（除非必要）。** 队友维护。上传冲突规则：**只有值班人可以上传。**

| 云函数 | 用途 |
|---|---|
| `skateActionAnalyze` | IMU 分析主云函数 — 帧分析、样本保存、去噪、MySQL |
| `predictAnalysis` | AI 推理代理 — 收帧 → HTTP 调 FastAPI `/infer` → 返回结果 |
| `quickstartFunctions` | 核心业务（79 handler） — 登录、社区、排课、班级等 |
| `mysqlBusiness` | MySQL 业务网关 — 登录、用户查询 |
| `saveSensorSamples` | 四节点静止采集专用存储 |
| `saveTrainingRecord` | 训练记录存储 |
| `login` | 微信 openid |
| `contentSecurityCheck` | 微信内容安全检测 |
| `addCommunityComment` | 社区评论 |
| `hunyuanVisionService` | 腾讯混元视觉 |
| `generateCoachQR` | 教练二维码生成 |

---

## 五、固件 — `firmware/firmware_v2_6_1/firmware_V2_7.21/`

### 固件列表

| 固件 | 角色 | 文件名 |
|---|---|---|
| `waist_imu_test/` | **腰部节点**（质心参考，网关兼 MQTT 主节点） | `waist_imu_test.ino` |
| `left_knee_imu_test/` | **左膝** | `left_knee_imu_test.ino` |
| `right_knee_imu_test/` | **右膝** | `right_knee_imu_test.ino` |
| `left_ankle_imu_test/` | **左踝/左脚** | `left_ankle_imu_test.ino` |
| `right_ankle_imu_test/` | **右踝/右脚** | `right_ankle_imu_test.ino` |

### 关键行为（逐条说明）

| 行为 | 说明 |
|---|---|
| **上电不自动校准** | 取消无条件接受 100 帧陀螺仪均值，改为等待 MQTT 命令：`"startup calibration disabled; waiting for UI command"` |
| **主动校准触发** | MQTT topic `sensor/imu/commands/{role}`，payload `"calibrate_gyro"` |
| **静止判断（4 条件全满足才合格）** | ① `gyro_norm ≤ CAL_MAX_GYRO_NORM_DPS`（角速度模长阈值）② `gyro_std_max ≤ CAL_MAX_GYRO_STD_DPS`（三轴标准差最大值）③ `acc_norm` 在 `[0.9, 1.1] g` 范围内 ④ `acc_norm_std ≤ CAL_MAX_ACCEL_NORM_STD_G`（加速度模长标准差）|
| **校准合格** | `gyroCalibrationValid = true`，MQTT 帧 `"status": "ready"`，offset 存入 RAM 临时变量 |
| **校准不合格** | `gyroCalibrationValid = false`，offset 清零，**不再恢复旧值**，MQTT 帧 `"status": "invalid"`。让 Python `denoise.py` 走试验台 bench offset |
| **清除校准** | `/calibration/gyro/clear` → `gyroCalibrationValid = false`，offset 清零，断电后自然失效 |
| **断线重连** | WiFi 连接失败后支持后台重连，不阻塞主循环 |
| **MQTT 帧上报字段（8 个）** | `status`, `sample_count`, `gyro_offset[x,y,z]`, `gyro_std_max`, `acc_norm_mean`, `acc_norm_std`, `temperature_c`, `filter_status` |
| **硬件 DLPF** | ICM20602 内置数字低通滤波器保持开启，陀螺仪约 **20 Hz**，加速度计约 **21.2 Hz** |

### 校准状态上报格式

固件校准完成后通过 MQTT 帧的 `calibration` 字段上报：

```json
{
  "calibration": {
    "status": "ready",
    "sample_count": 100,
    "gyro_offset": [0.52, -0.31, 0.18],
    "gyro_std_max": 0.87,
    "acc_norm_mean": 1.002,
    "acc_norm_std": 0.015,
    "temperature_c": 28.5,
    "filter_status": "dlpf_enabled"
  }
}
```

---

## 六、静态样本 — `static_samples/`

> ⚠️ 此目录在本地磁盘上已不存在，保留作为采集参考。

| 子目录 | 内容 | 说明 |
|---|---|---|
| `V2/` | 五节点冷启动静止数据（旧版） | 用于试验台校准分析 |
| `V3/waist_cold_start/` | V3 腰部冷启动静止数据 | 腰部单独采集合 |
| `V3/4nodes_cold_start/` | V3 四子节点冷启动静止数据 | 左/右膝、左/右踝 |

对应的试验台校准数据在根目录 `calibration_profiles_v3_raw*.json` 以及 `skating-deep-cnn-lstm-bayes/experiments/imu_calibration/` 中。

---

## 七、数据库 — `database/`

| 文件 | 内容 |
|---|---|
| `tencent-cloud-mysql-schema.sql` | MySQL 完整业务 schema（users, user_coach_relations, user_admin_grants 等） |

---

## 八、历史参考 — `model_service_fastapi/`

旧版 Python 端。**不再用于生产**，保留供参考：

| 文件 | 说明 |
|---|---|
| `src/api.py`（1688 行） | 旧版 FastAPI — 含 RF 基线模型代码（已废弃） + 传感器桥接代码（已迁移到 sensor_api.py） |
| `DEPLOY_CLOUD_SERVER.md` | 旧部署文档（路径指向 model_service_fastapi，已更新指向 skating-deep-cnn-lstm-bayes） |

---

## 九、数据链路全景

### 训练数据采集

```text
ICM20602（5 节点，硬件 DLPF 约 20 Hz）
  → ESP32-S3（50 Hz 采样）
  → MQTT Broker 82.156.18.205:1883
  → skating-deep-cnn-lstm-bayes/src/mqtt_client.py（帧缓冲）
  → FastAPI /frames（assembleNodes=true, assemblyToleranceMs=25）
  → device-sensor-adapter.js（去重、对齐）
  → debug.js（采集、标注）
  → sensor-model.js → skateActionAnalyze 云函数
  → train_samples_nofiltering（原始集合，不可覆盖）
  → FastAPI /denoise/training-frames（尖峰 + 可选低通）
  → train_samples_filtering（派生集合）
```

### 主动校准

```text
debug.js[onTapCalibrateSelectedNodes]
  → 弹窗确认 → 3 秒倒计时
  → POST /calibration/gyro(roles)
  → get_active_roles() 过滤在线节点
  → publish_gyro_calibration_command(roles) → MQTT → 固件
  → 固件 calibrateGyroscope()
      ├─ 合格 → gyroCalibrationValid=true, 帧 status="ready"
      └─ 不合格 → offset=0, gyroCalibrationValid=false, 帧 status="invalid"
  → 前端等 5 秒 → 轮询 /frames（8 轮 × 1 秒）
  → 读取 frame.calibration.status
  → calibrationProfiles[role] = {...}
  → 保存样本时传给 skateActionAnalyze
```

### AI 推理

```text
小程序 → remote-predict.js → predictAnalysis 云函数
  → HTTP POST → FastAPI /infer
  → _payload_to_record() 格式转换
  → predict_record() → CNN-LSTM + LightGBM
  → _deep_result_to_analysis() 格式转换
  → 返回 analysis
```

### 用户登录

```text
login 页面 → mysqlBusiness 云函数（loginByPhonePassword / loginWechat）→ MySQL users 表
          → quickstartFunctions 云函数（getOpenId）→ 文档型数据库
```

---

## 十、启动清单

| 组件 | 命令/操作 |
|---|---|
| Python 端 | `cd skating-deep-cnn-lstm-bayes && uvicorn src.sensor_api:app --host 0.0.0.0 --port 18080` |
| 微信开发者工具 | 打开 `posture-system/` 根目录，编译，调试 |
| 云函数部署 | 右键 → 上传并部署 |
| 小程序真机 WiFi 直连 | `config.js` 中 `wifi.apiBaseUrl` 设为 Python 端 IP:18080 |

---

## 十一、依赖汇总

### Python

`skating-deep-cnn-lstm-bayes/requirements.txt`：

```
torch>=2.0.0, numpy>=1.21.0, pandas>=1.3.0, scikit-learn>=1.0.0,
joblib>=1.2.0, tqdm>=4.62.0, fastapi>=0.100.0, uvicorn>=0.23.0,
lightgbm>=4.0.0, paho-mqtt>=1.6, scipy>=1.10
```

### 云函数

全部使用 `wx-server-sdk`，无其他外部依赖。`addCommunityComment` 的 `wx-server-sdk` 版本已从不存在的 `~2.11.0` 修正。

### 固件

Arduino ESP32-S3 + ICM20602 + PubSubClient (MQTT) + WiFi + NTP。
